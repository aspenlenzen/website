# Project notes

- The "Organizations I've Partnered With" logo marquee appears on both `index.dc.html` and `Resume.dc.html`. Any change to one (logos, copy, styling, speed) must be applied to the other so they stay identical.
- The footer (`<footer id="contact">`) appears on `index.dc.html`, `Resume.dc.html`, and `Consulting.dc.html`. Shared styling — footlinks heading size/weight, active-page link treatment (underline, not red fill), button styling — must stay identical across all three. Each page's contact-panel content (form fields vs. mailto CTA) and active-page link stay page-specific.
