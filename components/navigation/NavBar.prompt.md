Top nav with serif initials at left and centered links; active link gets a clay underline. No logo asset exists — use initials/name in type.

```jsx
<NavBar brand="ASL" links={["Home", "Resume"]} active="Home" onNavigate={setPage} />
```
